import assets from '@/public/assets';
import { companiesType } from '../types/sections';

export const companies: companiesType[] = [{ img: assets.amazon }, { img: assets.dribble }, { img: assets.hubspot }, { img: assets.notion }, { img: assets.netflix }, { img: assets.zoom }];
